import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angularMaterials',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
